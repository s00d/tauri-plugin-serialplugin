package com.hoho.android.usbserial;

/** Stub for vendored sources merged into the Tauri plugin module (no separate :usbserial project). */
public final class BuildConfig {
    public static final boolean DEBUG = false;
}
